[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = ["integration: requires a five-validator GenLayer environment"]
norecursedirs = [".venv", "node_modules", ".artifacts"]
